<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Glass Morphism</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <section>
        <div>
            <h1>ToDo List</h1>
            <div>
                <form method="post">
                    <% for (let i=0; i<items.length; i++){  %>
                        <input type="checkbox">
                        <p><%= items[i] %></p><br>
                    <% } %>
                    <div class="input">
                        <form method="post">
                        <input type="text" placeholder="add ToDo" name="newItem" required autocomplete="off">
                        <button type="submit">+</button>
                        </form>
                    </div> 
                </form>
            </div>
            
        </div>
    </section>
</body>
</html>
